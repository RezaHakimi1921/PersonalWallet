# آخرین چت — خلاصه سشن (2026-09-22)

این فایل خلاصه‌ی کاری‌ست که در آخرین سشن روی پروژه‌ی PersonalWallet انجام شد، تا در یک سیستم/سشن دیگر بشود از همین‌جا ادامه داد.

## زمینه‌ی پروژه
- بک‌اند: Node/Express + PostgreSQL، روی VPS با Docker Compose (`db`, `api`, `caddy`).
- سرور: `78.39.51.105`، دامنه‌ی HTTPS از طریق `https://78-39-51-105.nip.io` (Caddy + Let's Encrypt).
- دسترسی SSH به سرور: **پورت 2238** (نه 22!)، کاربر `root`، کلید اختصاصی در `~/.ssh/personalwallet_key`.
- مسیر پروژه روی سرور: `/home/personalwallet/personalwallet`.
- دیتابیس: `POSTGRES_USER=pw`, `POSTGRES_DB=personalwallet` (پسورد در `.env` سرور).
- روش دیپلوی: هیچ گیت روی سرور نیست؛ فایل‌ها با `scp` به `/home/personalwallet/upload_tmp_/` آپلود و از آنجا `cp` به مسیر پروژه می‌شوند، بعد `docker compose up -d --build api` برای بک‌اند (پی‌دبلیوای نیاز به build ندارد، فقط کپی فایل کافیست).
- فرانت‌اند PWA وانیلا جاوااسکریپت است، بدون فریم‌ورک، تقویم شمسی خودساخته (بدون وابستگی به CDN) در `app.js`.
- الگوی UTF-8: هیچ‌وقت از `curl -d` مستقیم در git-bash برای متن فارسی استفاده نشود (کاراکترها خراب می‌شوند)؛ باید از طریق فایل JSON نوشته‌شده + آپلود استفاده کرد.

## کارهایی که در این سشن انجام و دیپلوی و تست و پوش شد

### ۱. تفکیک دسته‌ی «تسویه بدهی/طلب» به دو دسته‌ی جهت‌دار
- قبلاً یک دسته‌ی مشترک برای هر دو جهت بود؛ الان:
  - **«تسویه طلب»** → فقط در تراکنش‌های واریزی (income) نمایش داده می‌شود، و لیست طلب‌های باز (کسانی که از آن‌ها طلب داری) را برای انتخاب می‌آورد.
  - **«تسویه بدهی»** → فقط در تراکنش‌های خرجی (expense) نمایش داده می‌شود، و لیست بدهی‌های باز (کسانی که به آن‌ها بدهکاری) را می‌آورد.
- این منطق در هر سه محل ثبت تراکنش وصل شده: تأیید تراکنش در انتظار، ثبت دستی، و ویرایش تراکنش.
- بک‌اند: اندپوینت جدید `POST /debts/:id/repay` با بدنه‌ی `{ amount_rial }` — از مبلغ بدهی/طلب کم می‌کند و اگر به صفر برسد به‌صورت خودکار `status = 'settled'` می‌شود.
- دسته‌های قبلی در دیتابیس (`id=21` income، `id=22` expense) که قبلاً با نام مشترک `تسویه بدهی/طلب` ساخته شده بودند، به نام‌های جدید rename شدند (نه حذف/بازسازی) تا تراکنش‌های قبلی مرتبط دست‌نخورده بمانند.
- تست شد: یک طلب تستی ۱,۰۰۰,۰۰۰ ریالی ساخته شد، ۴۰۰,۰۰۰ ریال تسویه شد، مبلغ باقی‌مانده درست ۶۰۰,۰۰۰ شد و رکورد باز ماند (چون کامل تسویه نشده). داده‌ی تست پاک شد.

### ۲. ماژول یادآوری (کاملاً جدید)
- جدول جدید `reminders` در دیتابیس — **self-migrating**: خودِ سرور موقع بالا آمدن اگر جدول نبود می‌سازدش (نیاز به مایگریشن دستی نیست). ستون‌ها: `id, module, title, note, remind_at (timestamptz), sent, created_at, deleted_at`.
- اندپوینت‌های REST: `GET /reminders`, `GET /reminders/trash`, `POST /reminders`, `PUT /reminders/:id`, `DELETE /reminders/:id` (soft-delete)، `POST /reminders/:id/restore`.
- کرون جدید هر ۵ دقیقه (`*/5 * * * *`) چک می‌کند یادآوری‌های سررسیدشده و ارسال‌نشده را، پوش نوتیفیکیشن ntfy می‌فرستد و `sent = true` می‌کند.
- فرانت‌اند: تب جدید «⏰ یادآوری‌ها» در منوی بالا و منوی «بیشتر». فرم ثبت شامل:
  - انتخاب ماژول (عمومی / قسط / بدهی و طلب / قبض / سایر) — طراحی‌شده تا بعداً بقیه‌ی بخش‌های برنامه (مثل اقساط) هم بتوانند یادآوری‌هایشان را زیر همین ماژول‌ها لیست کنند.
  - عنوان، توضیح اختیاری.
  - **تقویم شمسیِ واقعی و اینلاین** (تابع `createJalaliCalendar` در `app.js`) — گرید روزهای ماه با ناوبری ماه قبل/بعد، بدون وابستگی به تقویم میلادیِ اینپوت native مرورگر.
  - انتخاب ساعت دقیق (`<input type="time">`).
  - قابل ویرایش (`openReminderEditModal`) و حذف با بازیابی از سطل بازیابی (تب Trash هم آپدیت شد).
- تست شد: یک یادآوری با `remind_at` در گذشته ساخته شد؛ بعد از یک تیک کرون (۵ دقیقه)، `sent` به `true` تغییر کرد یعنی پوش نوتیفیکیشن درست ارسال شده. داده‌ی تست پاک شد.

## فایل‌های تغییریافته
- `api/src/index.js` — اندپوینت‌های `/debts/:id/repay`، ماژول کامل `reminders` (migration + CRUD + cron).
- `pwa/app.js` — split دسته‌های تسویه، helper های `repayFieldHtml/wireRepayField/maybeRepayDebt`، تابع `createJalaliCalendar`، `renderReminders`/`openReminderEditModal`، آپدیت `renderTrash`.
- `pwa/index.html` — تب و آیتم منوی «یادآوری‌ها».
- `db/init.sql` — جدول `reminders` برای نصب‌های تازه (fresh install).

## وضعیت گیت
- همه‌چیز کامیت و پوش شده روی `origin/main`، کامیت: `057db23` — «Add debt repayment linking and a generic reminders module».
- کامیت قبلی مرتبط: `6758dbe` (رفع باگ‌های مصرف/داشبورد/دکمه‌های تکراری/حریم خصوصی) و `747f846` (اتصال قرض به ماژول بدهی/طلب).

## نکات مهم برای سشن بعدی
- برای دیپلوی حتماً از پورت SSH **2238** و کلید `~/.ssh/personalwallet_key` استفاده شود (پورت 22 بسته است).
- مسیر پروژه روی سرور گیت ندارد؛ فقط با `scp`/`cp` دستی سینک می‌شود.
- هیچ آیتم باز/ناتمامی از این سشن باقی نمانده — همه‌چیز دیپلوی، تست و پوش شده.
